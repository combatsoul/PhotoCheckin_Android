<menu xmlns:android="http://schemas.android.com/apk/res/android" >

    <item
        android:id="@+id/checkin_photo"
        android:icon="@drawable/checkinphoto_icon"
        android:orderInCategory="50"
        android:showAsAction="always"/>

    <!-- submenu -->
    <item
        android:icon="@drawable/navlist_menu"
        android:orderInCategory="50"
        android:showAsAction="always">
        <menu>
        <item
            android:id="@+id/createactivity"
            android:orderInCategory="50"
            android:showAsAction="never"
              android:icon="@drawable/createactivity_icon"
            android:title="Create Activity"/>
        <item
            android:id="@+id/profile"
            android:orderInCategory="50"
            android:showAsAction="never"
            android:icon="@drawable/viewprofile_icon"
            android:title="Edit Profile"/>
        <item
            android:id="@+id/Logout"
            android:icon="@drawable/logout_icon"
            android:orderInCategory="50"
            android:showAsAction="never"
            android:title="Logout"/>
        </menu>
    </item>

</menu>